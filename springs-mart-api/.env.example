# Port for local development (ignored on Vercel, which injects its own)
PORT=4000

# Comma-separated list of allowed origins for CORS.
# Use "*" to allow all origins (fine for quick demos, not recommended for production).
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
